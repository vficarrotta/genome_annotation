At present contains a vetted install script for a genome annotation software stack that avoids perl vs python collisions on an HPC.
It also circumvents the progress bar install issue with AGAT (perl). 
